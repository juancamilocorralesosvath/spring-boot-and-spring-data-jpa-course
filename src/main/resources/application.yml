my:
  custom:
    property: "hello world in production"
  second:
    prop: "bye in production"
spring:
  application:
    name: pioHealth
  datasource:
    url: jdbc:postgresql://localhost:5433/trial-db
    username: postgres
    password: hola1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
